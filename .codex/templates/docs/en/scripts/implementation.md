# Implementation

Scripts are small Python files. codex_state.py manages workflow state.
codex_trace_view.py renders TASKS.md. codex_task_git.py links commits.
codex_quality.py scans changed files. codex_shortcuts.py blocks shortcut prompts
after running the matching script.
