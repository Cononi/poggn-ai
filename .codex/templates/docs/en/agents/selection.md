# Agent selection

Codex recommends agents from request text. The user selects or adds agents.
The system does not ask for an agent count. Work items create feature lanes.

```text
$codex-agents recommend --text "shop order payment rest api"
```
